import { ValidationGuardsGuard } from './validation.guards.guard';

describe('ValidationGuardsGuard', () => {
  it('should be defined', () => {
    expect(new ValidationGuardsGuard()).toBeDefined();
  });
});
